"""Grounded natural-language generation and querying over structured
security events -- the NLP layer, kept deliberately separate from the
CV/tracking code so it can be developed, tested, and evaluated against
its own dataset independent of the camera pipeline.
"""
